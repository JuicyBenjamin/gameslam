import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class="flex h-[50vh] flex-col items-center justify-center p-4">
      <h1>We failed to confirm your email, get fuckedd</h1>
      <p>Sorry it might be because we are on the free tier</p>
    </div>
  );
});
